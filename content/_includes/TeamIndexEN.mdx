---
layout: Team.tsx
title: team
lang: en
id: teamPage
blog: false
---

<div className="team-container">
  <h1>The Team</h1>

  <section className='team-section'>
    <h2>Organization</h2>
    <p>Every project needs people who make it possible. Here are ours:</p>
    <div className="team-card-container">
      {page.data.team.orga.length > 0 ? (
        page.data.team.orga.map((teamMember) => (
          <div key={teamMember.name} className="team-card team-three-row">
            <div className="team-image-container">
              <picture className="team-picture">
                <source
                  srcSet={`${teamMember.picture?.replace(/\.(jpg|jpeg|png)$/, ".avif") || "/media/teamPictures/kitty.avif"}`}
                  type="image/avif"
                />
                <img
                  src={teamMember.picture || "/media/teamPictures/kitty.jpg"}
                  alt={`${teamMember.name}'s picture`}
                  className="team-image"/>
              </picture>
            </div>
            <div className="team-card-body">
              <h3 className="team-name">{teamMember.name}</h3>
              <h4 className="team-title">{teamMember.title}</h4>
              {teamMember.socialLinks && (
                <ul className="team-socialLinks">
                  {Object.entries(teamMember.socialLinks).map(([platform, url]) => (
                    <li key={platform}>
                      <a href={url} className="team-link">{platform}</a>
                    </li>
                  ))}
                </ul>
              )}
            </div>
          </div>
        ))
      ) : (
        <p className="team-noData">No organization members available.</p>
      )}
    </div>
  </section>

  <section className='team-section'>
    <h2>Advisory Board</h2>
    <p>Our advisory board not only decides which projects are funded, but is also active in various local and national CCC groups.</p>
    <p>The advisory board can always be reached at ccs@chaostreff-flensburg.de. All questions are welcome!</p>
    <div className="team-card-container">
      {page.data.team.beirat.length > 0 ? (
        page.data.team.beirat.map((teamMember) => (
          <div key={teamMember.name} className="team-card team-three-row">
            <div className="team-image-container">
              <picture className="team-picture">
                <source
                  srcSet={`${teamMember.picture?.replace(/\.(jpg|jpeg|png)$/, ".avif") || "/media/teamPictures/kitty.avif"}`}
                  type="image/avif"
                />
                <img
                  src={teamMember.picture || "/media/teamPictures/kitty.jpg"}
                  alt={`${teamMember.name}'s picture`}
                  className="team-image"/>
              </picture>
            </div>
            <div className="team-card-body">
              <h3 className="team-name">{teamMember.name}</h3>
              <h4 className="team-title">{teamMember.title}</h4>
              {teamMember.socialLinks && (
                <ul className="team-socialLinks">
                  {Object.entries(teamMember.socialLinks).map(([platform, url]) => (
                    <li key={platform}>
                      <a href={url} className="team-link">{platform}</a>
                    </li>
                  ))}
                </ul>
              )}
            </div>
          </div>
        ))
      ) : (
        <p className="team-noData">No advisory board members available.</p>
      )}
    </div>
  </section>

  <section className='team-section'>
    <h2>Our Mentors</h2>
    <p>Our mentors also come from the Chaos Computer Club community and volunteer their time to support our scholarship recipients with advice and guidance.</p>
    <div className="team-card-container">
      {page.data.team.mentor.length > 0 ? (
        page.data.team.mentor.map((teamMember) => (
          <div key={teamMember.name} className="team-card team-three-row">
            <div className="team-image-container">
              <picture className="team-picture">
                <source
                  srcSet={`${teamMember.picture?.replace(/\.(jpg|jpeg|png)$/, ".avif") || "/media/teamPictures/kitty.avif"}`}
                  type="image/avif"
                />
                <img
                  src={teamMember.picture || "/media/teamPictures/kitty.jpg"}
                  alt={`${teamMember.name}'s picture`}
                  className="team-image"/>
              </picture>
            </div>
            <div className="team-card-body">
              <h3 className="team-name">{teamMember.name}</h3>
              <h4 className="team-title">{teamMember.title}</h4>
              {teamMember.socialLinks && (
                <ul className="team-socialLinks">
                  {Object.entries(teamMember.socialLinks).map(([platform, url]) => (
                    <li key={platform}>
                      <a href={url} className="team-link">{platform}</a>
                    </li>
                  ))}
                </ul>
              )}
            </div>
          </div>
        ))
      ) : (
        <p className="team-noData">No mentors available.</p>
      )}
    </div>
  </section>

  <section className='team-section'>
    <h2>Our Alumni</h2>
    <p>Receiving a scholarship is already challenging enough, and then comes writing the master thesis. We are happy to have supported the following individuals:</p>
    <div className="team-card-container">
      {page.data.team.alumni.length > 0 ? (
        page.data.team.alumni.map((teamMember) => (
          <div key={teamMember.name} className="team-card team-three-row">
            <p className="team-year">{teamMember.year}</p>
            {teamMember.picture && (<img
              src={teamMember.picture}
              alt={`${teamMember.name}'s picture`}
              className="team-image"
            />)}
            <div className="team-card-body w-md-auto">
              <h3 className="team-name">{teamMember.name}</h3>
              {teamMember.thesis && (
                <div>
                  <p className='team-thesis'><strong>Master Thesis: </strong>"{teamMember.title}"</p>
                  <a href={teamMember.thesis} className="team-linkUnderline">read now</a>
                </div>
              )}
              {teamMember.socialLinks && (
                <ul className="team-socialLinks">
                  {Object.entries(teamMember.socialLinks).map(([platform, url]) => (
                    <li key={platform}>
                      <a href={url} className="team-link">{platform}</a>
                    </li>
                  ))}
                </ul>
              )}
              {teamMember.position && <p className="team-info mt-2">{teamMember.position}</p>}
            </div>
          </div>
        ))
      ) : (
        <p className="team-noData">No alumni members available.</p>
      )}
    </div>
  </section>
</div>
