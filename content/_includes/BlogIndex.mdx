---
layout: Blog.tsx
title: Blog
lang: de
id: blogpage
blog: true
---

<div>
  {page.data.results.map((page) => (
    <div class="blog-card">
      <div class="blog-image-container">
        <img src={page.data.img ? page.data.img : "/media/blog/ccs.png"} alt={page.data.alt} class="blog-image"/>
      </div>
      <div>
        <div class="blog-title-container">
          <h2 class="text-xl font-medium blog-title">{page.data.title}</h2>
          <span>
            {new Date(page.data.date).toLocaleDateString("de-DE", {
            year: "numeric",
            month: "numeric",
            day: "numeric",
            })}
          </span>
        </div>
        <div class="blog-text-container">
          <p class="m-1">{page.data.excerpt}</p>
          <a href={page.data.url} class="blog-link">Weiterlesen</a>
        </div>
      </div>
    </div>
  ))}
</div>

<nav>
  <ul class="pagination">
    {page.data.pagination.previous ? (
      <li class="page-item">
        <a class="page-link" href={`/blog/${page.data.pagination.previous}`}>
          &laquo;
        </a>
      </li>
    ) : (
      <li class="page-item">
        <a class="page-link disabled">&laquo;</a>
      </li>
    )}
    {new Array(page.data.pagination.totalPages).fill().map((_, index) => (
      <li class="page-item">
        <a class="page-link" href={`/blog/page/${index + 1}`}>
          {index + 1}
        </a>
      </li>
    ))}
    {page.data.pagination.next ? (
      <li class="page-item">
        <a class="page-link" href={`/blog/${page.data.pagination.next}`}>
          &raquo;
        </a>
      </li>
    ) : (
      <li class="page-item">
        <a class="page-link disabled">&raquo;</a>
      </li>
    )}
  </ul>
</nav>
